# @package _global_
defaults:
  - override /model: mistral_7b_instruct
  - override /data: mediqa
  - override /trainer: 0_shot


hydra:
  run:
    dir: outputs/mistral_7b_instruct/${now:%Y-%m-%d}/${now:%H-%M-%S}